name: Integration tests

on:
  pull_request:
  schedule:
    - cron: "0 15 * * SAT"
  merge_group:

jobs:
  integration-tests-juju-3:
    uses: canonical/operator-workflows/.github/workflows/integration_test.yaml@main
    secrets: inherit
    permissions:
      packages: write
    with:
      trivy-fs-enabled: false
      trivy-image-config: "trivy.yaml"
      self-hosted-runner: true
      juju-channel: "3/stable"
      channel: "1.36-strict/stable"
      microk8s-addons: "dns ingress rbac storage metallb:10.64.140.43-10.64.140.49"
      modules: '["test_charm", "test_git_sync", "test_oauth", "test_s3", "test_saml", "test_smtp"]'
      with-uv: true
  integration-tests:
    uses: canonical/operator-workflows/.github/workflows/integration_test.yaml@main
    secrets: inherit
    if: false # The MySQL-K8s charm does not support Juju 4 yet
    permissions:
      packages: write
    with:
      trivy-fs-enabled: true
      trivy-image-config: "trivy.yaml"
      self-hosted-runner: true
      juju-channel: "4/stable"
      channel: "1.36-strict/stable"
      modules: '["test_charm", "test_git_sync", "test_oauth", "test_s3", "test_saml", "test_smtp"]'
      with-uv: true
  allure-report:
    if: ${{ !cancelled() && github.event_name == 'schedule' }}
    needs:
      - integration-tests-juju-3
      - integration-tests
    permissions:
      contents: write
    uses: canonical/operator-workflows/.github/workflows/allure_report.yaml@main
